# Implementation plan

1. Pin and verify official GEPA `v0.1.1` at the required commit and import path.
2. Define typed protocol, canonical bundle, strict parser, provider, and budget contracts.
3. Implement the current-member-only evaluator and the fixed-version GEPA adapter.
4. Implement five isolated member runs, checkpoint identity checks, and direct final-team composition.
5. Implement frozen-prompt plurality evaluation and aligned aggregate diagnostics.
6. Implement private/public artifact boundaries, sanitization, and leakage audits.
7. Add deterministic fake models and full offline coverage, including a five-member end-to-end run.
8. Run every offline gate and update `IMPLEMENTATION_STATUS.md`.

Real task or reflection model calls are outside stage one and remain disabled.
